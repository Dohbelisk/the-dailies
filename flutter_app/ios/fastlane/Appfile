# App Store Connect App Configuration
# For more information about the Appfile, see:
# https://docs.fastlane.tools/advanced/#appfile

app_identifier("com.dohbelisk.thedailies")  # Bundle identifier
apple_id(ENV["APPLE_ID"])                   # Your Apple Developer email (set via environment)
team_id("95AHS9L6HZ")                       # Developer Portal Team ID
itc_team_id(ENV["ITC_TEAM_ID"])             # App Store Connect Team ID (set via environment)

# For App Store Connect API Key authentication (recommended for CI/CD)
# Generate at: https://appstoreconnect.apple.com/access/api
# Then set these environment variables:
#   APP_STORE_CONNECT_API_KEY_KEY_ID
#   APP_STORE_CONNECT_API_KEY_ISSUER_ID
#   APP_STORE_CONNECT_API_KEY_KEY (base64 encoded .p8 file contents)
